# Инструкция по деплою на GitHub Pages

## ✅ Что было исправлено:

1. **Добавлен `base: '/Monetrix/'` в `vite.config.ts`** - это нужно для правильной работы путей на GitHub Pages
2. **Добавлен `basename="/Monetrix"` в `BrowserRouter`** - для правильной работы React Router на GitHub Pages

## 📋 Шаги для деплоя:

### 1. Убедитесь, что проект собран с новыми настройками:

```bash
npm run build
```

Это создаст папку `dist` с правильными путями.

### 2. Закоммитьте изменения в репозиторий:

```bash
git add .
git commit -m "Configure GitHub Pages deployment"
git push
```

### 3. Задеплойте на GitHub Pages:

```bash
npm run deploy
```

Эта команда:
- Автоматически запустит `npm run build` (через `predeploy`)
- Создаст ветку `gh-pages` в вашем репозитории
- Загрузит содержимое папки `dist` в эту ветку

### 4. Настройте GitHub Pages в настройках репозитория:

1. Перейдите в ваш репозиторий на GitHub
2. Откройте **Settings** → **Pages**
3. В разделе **Source** выберите:
   - **Branch**: `gh-pages`
   - **Folder**: `/ (root)`
4. Нажмите **Save**

### 5. Подождите несколько минут

GitHub Pages обычно активируется в течение 1-5 минут. Ваш сайт будет доступен по адресу:
**https://Brostall.github.io/Monetrix/**

## 🔄 Обновление сайта:

Каждый раз, когда вы хотите обновить сайт на GitHub Pages:

```bash
npm run deploy
```

Это автоматически пересоберет проект и обновит ветку `gh-pages`.

## ⚠️ Важные замечания:

1. **Папка `dist` должна оставаться в `.gitignore`** - это правильно! Она не должна попадать в основную ветку, так как `gh-pages` создает отдельную ветку для деплоя.

2. **Если вы измените название репозитория или username**, не забудьте обновить:
   - `base` в `vite.config.ts`
   - `basename` в `src/main.tsx`
   - `homepage` в `package.json`

3. **Для локальной разработки** используйте `npm run dev` - он работает без base path.

## 🐛 Если что-то не работает:

1. Проверьте, что ветка `gh-pages` создана в репозитории
2. Убедитесь, что в настройках GitHub Pages выбран правильный источник
3. Проверьте консоль браузера на наличие ошибок 404
4. Убедитесь, что все пути в коде используют относительные пути или правильный base path

